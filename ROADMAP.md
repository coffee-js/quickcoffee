# QuickCoffee 后续开发路线图

- 基线：`main`，0.1 语义与工具 RFC 0000–0117 已实现
- 性质：非规范性规划；具体语义、字节码或公开 API 变更仍须先提交 RFC
- 原则：不兼容 JavaScript，不引入原型链、`this`、隐式类型转换或内嵌 JavaScript

## 现状判断

QuickCoffee 已具备可执行的单文件语言核心、经验证字节码、fuel 限制 VM、四个 CLI、Rust 嵌入 API、五种语言手册和 release 性能门禁。当前主要风险不再是“缺少若干语法糖”，而是：源码与运行时结构仍偏单体、跨文件程序没有正式模型、资源上限只有指令 fuel、字节码和宿主边界尚未形成稳定的发布契约，性能数据也尚未转化为可自动判断的回归基线。

后续工作分为六块。每块应由一个总 RFC 确定边界，再拆成短小、可独立验收的 PR；禁止在同一 PR 中同时改语言语义、字节码格式和 CLI 契约。

## A. 前端架构与 CoffeeScript 2016 范围收敛

### 目标

建立带完整源码范围的词法、AST、诊断和编译流水线，并对 CoffeeScript 2016 特性逐项作出“实现、改写或明确拒绝”的决定。保持 QuickCoffee 的严格值模型，不以 JavaScript 兼容为目标。

### 主要需求

1. 为 token、AST 节点和编译错误保存起止位置、文件名与上下文片段，支持一条错误中的主标注和辅助标注。
2. 将当前大型 lexer、parser、compiler 文件按布局、表达式、模式、控制流和 lowering 职责拆分，减少跨阶段隐式约定。
3. 建立 CoffeeScript 2016 特性矩阵；优先评估对象 shorthand 边界、复杂调用/换行、类语法扩展和模式语法，继续拒绝反引号 JavaScript。
4. 为解析器增加确定性错误恢复，使 `--check` 可一次报告多个互不依赖的语法错误；执行模式仍在第一条错误处停止。
5. 定义 QuickCoffee 自有源码映射格式，服务诊断和反汇编，不生成 JavaScript source map。

### 验收

- 所有 Parse/Verify/Runtime 错误都可携带文件与精确范围，现有 `ErrorKind` 兼容。
- 特性矩阵中的每项都有 RFC、正例、反例或明确非目标说明。
- 前端拆分前后的现有字节码指纹不变；有意变更必须升级字节码版本。
- 解析压力语料和 fuzz 输入不得 panic，错误顺序稳定。

## B. 模块、加载器与可复用程序包

### 目标

从单文件脚本扩展为可嵌入、可缓存的多文件程序，同时把文件系统和网络能力留在宿主侧。

### 主要需求

1. 设计无副作用歧义的 `import`/`export` 语义、循环依赖规则、模块初始化顺序和模块级私有环境。
2. 提供 `ModuleLoader` 宿主接口；引擎只接收规范化模块名和源码/字节码，不自行访问文件系统或网络。
3. CLI 文件加载器定义相对路径、搜索根、扩展名、重复加载缓存和目录穿越保护。
4. 将 `Program` 扩展为模块图，缓存键至少包含规范模块名、源码摘要、引擎/字节码版本和编译选项。
5. 为预编译模块包定义确定性清单；签名、压缩和远程分发先留给宿主工具。

### 验收

- 内存加载器、受限文件加载器、循环依赖、缺失模块和重复导入均有集成测试。
- 同一模块图在相同版本上生成稳定指纹，任一依赖变化会改变图指纹。
- 嵌入方可完全禁用模块加载，或只允许显式白名单。
- qtest 可按模块包运行，每个用例仍有独立 fuel 和隔离全局环境。

## C. 字节码格式与 VM 性能工程

### 目标

把当前“Rust 枚举指令 + 名称映射”的正确实现演进为紧凑、可版本化、可测量的执行格式，在保持验证器和 fuel 语义的前提下降低分派、查名和分配成本。

### 主要需求

1. 记录基准剖析：指令分派、局部/全局查找、调用帧、字符串、数组、映射、异常和迭代分别占用的时间与分配。
2. 以局部槽位替代热路径中的字符串名称查找；全局与宿主名称保留显式、可审计的解析边界。
3. 评估紧凑字节码编码、常量/符号 intern、专用短指令和更少的临时集合；先写格式版本与验证规则，再实现编码器。
4. 增加可移植的字节码序列化/反序列化，包含 magic、版本、校验和、大小上限与递归深度上限；不序列化宿主函数。
5. 为优化建立差分执行：未优化与优化程序在值、错误种类、源码位置、fuel 规则上保持一致。

### 验收

- 反序列化任意输入不得 panic、越界分配或绕过验证。
- qbench 输出分配量或可替代的内存指标，并保留当前 schema 的版本升级策略。
- 选定核心负载在同一基准机上达到总体验收目标；目标值由性能 RFC 根据剖析结果确定，不预先承诺不可信倍数。
- 每项优化附带语义测试、微基准和 `PERFORMANCE.md` 前后对比。

## D. 内存、隔离与资源治理

### 目标

将“fuel 可终止执行”扩展为完整的嵌入式资源边界，解决闭包/环境环、深层值和大对象造成的不可控内存占用。

### 主要需求

1. 先建立分配计数和存活对象观测，确认 `Rc` 环、递归闭包与长期 `Context` 的实际保留行为。
2. 设计内存预算、最大调用深度、最大容器长度、最大字符串/模块/字节码大小和取消令牌。
3. 在“弱引用打断环境环、arena/代际句柄、追踪式 GC”之间做原型比较；最终方案不得向脚本暴露对象地址或原型链。
4. 定义一次执行失败后的上下文状态：资源耗尽必须返回结构化错误，不能留下半写入的宿主可见状态。
5. 增加隔离 Context/Runtime 的生命周期模型，为并发宿主提供明确的 `Send`/非 `Send` 契约，而非偶然依赖内部 `Rc`。

### 验收

- 无限循环、递归爆栈、超大容器、深层嵌套和闭包环都有确定性限制测试。
- 资源错误具有稳定 `ErrorKind`/子类型和统计数据；宿主可区分 fuel、内存、取消和深度限制。
- 长期运行的压力测试在完成回收或上下文销毁后回到可解释的内存范围。
- 所有不可信字节码入口先执行大小限制与验证。

## E. 嵌入 API 2.0 与宿主能力模型

### 目标

提供比 rquickjs 更小、更直观但足以用于真实系统的稳定 API：显式生命周期、能力注入、结构化转换和可取消执行。

### 主要需求

1. 引入 `RuntimeBuilder`/`ContextBuilder` 的清晰分层：运行时拥有共享编译缓存与资源策略，上下文拥有全局状态。
2. 为原生函数提供调用上下文，包括取消检查、资源统计和受控宿主状态；仍不暴露 VM 栈、环境或内部引用。
3. 增加 `TryFrom<Value>`/`IntoValue` 风格的严格转换，覆盖 tuple、option、数组、map 和用户可实现 trait；不做隐式数值或字符串转换。
4. 设计宿主对象句柄和 capability table，使文件、时钟、随机、网络等能力只能被显式注入。
5. 明确 panic 边界、错误链、线程模型、API 稳定级别和 semver 迁移策略。

### 验收

- 提供最小嵌入、共享程序、多上下文隔离、宿主状态、取消与资源限制示例。
- 公共 API 在 docs.rs 上零缺失文档，并有 compile-fail 或外部集成测试保护错误用法。
- 原生函数 panic 不得跨 FFI/服务边界破坏宿主进程；具体捕获策略由 RFC 决定。
- 0.2 稳定面形成兼容性清单，实验 API 使用明确命名或 feature gate。

## F. CLI、质量与发布工程

### 目标

把现有工具从开发可用提升到可分发、可诊断、可回归的产品级工具链。

### 主要需求

1. 增加 qcompile（或 qcoffee 编译模式）：生成版本化字节码包，并支持检查、反汇编和运行；原生单文件可执行打包另立 RFC。
2. 统一四个 CLI 的参数解析、错误 JSON schema、退出码、颜色/TTY 策略和 `--help` 文案。
3. 为 qtest 增加隔离临时目录、超时/取消、并行执行与 JUnit 输出；默认顺序和 JSON/TAP 仍保持确定性。
4. 为 qdocco 增加模板版本与增量生成；五种手册继续由同一语义测试驱动，生成物必须可重复。
5. 建立 fuzz、Miri/解释器适用检查、依赖审计、Linux/macOS/Windows 矩阵、发布制品校验和与 crates.io dry-run。
6. 将性能报告拆为可复现基线数据和叙述报告，CI 对显著回归给出告警；正式阻断阈值只在基准噪声模型稳定后启用。

### 验收

- 每个正式版本自动产出 crate、各平台 CLI、校验和、变更日志和可重跑的性能报告。
- CLI schema 与退出码有跨平台黄金测试；路径、非 UTF-8 名称和管道输入在支持的平台行为明确。
- fuzz corpus 覆盖 lexer、parser、验证器、字节码解码器和 VM，发现的崩溃必须固化为回归测试。
- `make check` 保持本地可运行；耗时较长的安全/性能任务进入独立 CI job。

## 推荐实施顺序

1. **0.2 基础契约**：A 的源码范围与诊断模型、D 的资源限制模型、C 的字节码版本头。三者先定接口，避免后续模块和序列化反复迁移。
2. **0.2 可嵌入运行时**：E 的 Runtime/Context 分层与取消能力，同时实现 D 的内存/深度限制。
3. **0.3 多文件程序**：B 的加载器、模块图和缓存；在此基础上交付 F 的预编译工具。
4. **0.3 性能迭代**：按剖析依次落地 C 的槽位、intern 和紧凑编码，每一步独立测量。
5. **持续工程**：F 的跨平台、fuzz、发布和性能告警贯穿所有里程碑，不集中到版本末尾补做。

## 拆分与治理规则

- 每个大需求先建立一个 tracking issue，正文链接总 RFC、子任务、依赖和验收状态。
- 每个 PR 只完成一个可验证切片，先处理 review comment，再保证本地门禁与 Actions 成功后合并。
- 新语言特性必须同时更新 RFC、中文/英文语法索引、五份可执行手册或说明其不适用原因。
- 新字节码必须更新验证器、反汇编、指纹/版本规则、恶意输入测试和性能报告。
- 新公开 API 必须更新 Rust 示例、外部集成测试和迁移说明。
- 路线图每个 minor 版本复审一次；已经采纳的语义只由 RFC 修订，不由本文件暗中改变。
