# RFC 0073：解构模式默认值

状态：实现中

数组与映射解构模式成员可写为 `pattern = expression`。输入成员缺失或为 `nil` 时，在当前词法环境中动态求值表达式；默认值可引用此前已绑定成员。多余数组成员、错误容器类型和无默认值的缺失成员均报错，且整个匹配原子回滚。

```coffee
[first = 1, second = first + 1] = [nil]
second # 2
```

默认表达式编译为独立字节码块，沿用执行燃料与统计计数。本 RFC 依赖 RFC 0023、RFC 0026 与 RFC 0071。
