# RFC 0075：映射解构尾部模式

状态：已采纳

映射解构可在末尾使用 `...name` 捕获未列出的键：`{id, ...metadata} = record`。显式字段先按普通严格模式匹配；其余键组成新的不可变映射绑定到 `name`。尾部模式必须命名且只能出现一次、位于映射模式末尾；输入必须是映射，缺失的显式字段仍报错并保持原子性。

该模式与 RFC 0071 的数组尾部模式互不混淆，编译为验证器可检查的 `MapRest` 字节码结构。
