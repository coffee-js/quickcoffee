# RFC 0078：字节码内容指纹

状态：实现中

`Chunk::fingerprint()` 与 `Program::fingerprint()` 返回由已编译字节码文本表示计算的确定性 FNV-1a `u64` 指纹。相同内容的共享程序和克隆句柄指纹相同，不同字节码内容应产生不同指纹（不把指纹当作密码学哈希）。

该接口供嵌入宿主建立内存缓存键、日志关联和编译结果诊断；它不改变验证、执行或 fuel 语义，也不暴露 VM 内部环境。
