# RFC 0102：qtest 文件规范路径去重

- 状态：已采纳
- 依赖：RFC 0077、RFC 0079

`qtest` 收集显式文件、目录递归结果和符号链接时，以 `fs::canonicalize` 得到的规范路径作为逻辑文件身份。同一测试文件即使通过多个相对路径或文件符号链接传入，也只执行一次；输出保留第一次被收集时的显示路径。目录仍按规范路径去重，以避免循环。

规范路径解析失败是收集错误。收集完成后仍按显示路径排序，因此 JSON 与 TAP 输出在输入顺序变化时保持确定。
